import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { Rect } from "react-konva";

<Meta title="react-konva-addons/Introduction" />

# Introduction

The `react-konva-addons` package contains some useful addon components and hooks
for `react-konva` that make it easier to build functionality for interactive
HTML5 canvas components in a declarative React-like style.
